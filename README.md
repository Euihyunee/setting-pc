# setting-pc

개인 PC 환경 설정 모음 (dotfiles, terminal, prompt, editor 등).

## 구성

- `ohmyposh/` — Oh My Posh 테마 및 커스텀 설정
- `powershell/` — PowerShell 프로필 (`$PROFILE`)
- `vscode/` — VS Code 설정 및 확장 목록
- `git/` — 전역 `.gitconfig`, `.gitignore_global`

## 사용

각 폴더 내부의 README 또는 스크립트를 참고하여 적용합니다.
